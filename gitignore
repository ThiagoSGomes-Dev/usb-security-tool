# Portable Python runtime — download manually from python.org
python/

# Auto-generated by SETUP.bat
Lib/

# Local install lock — specific to each USB drive
.installed

# Encrypted files — never commit personal data
*.enc

# Windows thumbnail cache
Thumbs.db
desktop.ini
